# Screener.in screen query for the Turnaround Hunter universe.
# Lines starting with # are ignored; remaining lines are joined with spaces, so
# you can split a long query across lines for readability.
#
# This runs server-side on screener.in to shrink the universe BEFORE per-company
# analysis: small/mid-cap band + positive operating profit that is improving YoY.
# turnaround.py then confirms the 52-week-low proximity and the 4-quarter EBITDA
# trend on each company page.
#
# Field names use screener.in's query language. If screener changes them and the
# screen returns nothing, the scan falls back to watchlist.txt — fix the fields here.

Market Capitalization < 75000 AND
Market Capitalization > 500 AND
Operating profit latest quarter > 0 AND
Operating profit latest quarter > Operating profit preceding year quarter
